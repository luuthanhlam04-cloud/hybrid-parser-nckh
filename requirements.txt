# ============================================================
# Requirements — hybrid_parser_graphrag
# Module 1: Document Preprocessing
# ============================================================

# --- Xử lý file DOCX ---
python-docx>=1.1.2          # Đọc cấu trúc .docx (dùng trong XML fallback)
pypandoc>=1.13              # Wrapper cho Pandoc — phục hồi auto-numbering (tầng chính)
lxml>=5.2.0                 # Phân tích XML word/numbering.xml (tầng dự phòng)

# --- Xử lý văn bản & Unicode ---
# unicodedata: thư viện chuẩn Python (không cần cài thêm)

# --- Tiện ích ---
python-dotenv>=1.0.0        # Quản lý biến môi trường từ .env

# ============================================================
# Các module sắp tới (placeholder cho Module 2+)
# ============================================================

# --- Module 2: Regex Parser ---
# (chỉ dùng thư viện chuẩn: re, json)

# --- Module 6: LLM Structured Extraction ---
openai>=1.30.0
instructor>=1.3.3
pydantic>=2.7.0
tenacity>=8.3.0

# --- Module 4: Neo4j Ingestion ---
# neo4j>=5.19.0

# --- Module 5: Đánh giá / Kiểm thử ---
pytest>=8.2.0
pytest-cov>=5.0.0
numpy>=1.26.0
sentence-transformers>=3.0.0

# --- Module 8: W3C SHACL validation (optional at runtime) ---
pyshacl>=0.26.0
rdflib>=7.0.0
neo4j>=5.19.0

# --- Module 7: Ontology Builder ---
PyYAML>=6.0
thefuzz[speedup]>=0.20.0
